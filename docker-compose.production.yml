services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile.production
    depends_on:
      - db
    ports:
      - "3000:3000"
  db:
    image: mongo  
    restart: always
    ports:
      - 27017:27017
    volumes:
      - mongodb_data:/data/db
volumes:
  mongodb_data:
